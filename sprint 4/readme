Проект 4: работа с реляционной базой данных такси. 
В процессе проекта было сделано: подключение к удаленному серверу; получение нужных логов(grep); создание директорий(mkdir); создание файлов внутри директорий; связывание таблиц(INNER JOIN); получение количества автомобилей из таблицы(COUNT); и решение других задач с использованием SQL-запросов(SELECT, FROM, WHERE, ORDER BY, GROUP BY, LIMIT, LIKE и другие).
